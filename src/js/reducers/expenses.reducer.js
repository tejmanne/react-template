const INITIAL_STATE = {}

export const Expenses = (state = INITIAL_STATE, action) => {
    switch (action.type) {
        case "":
            break;

        default:
            break;
    }

    return state;
}