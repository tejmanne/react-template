const INITIAL_STATE = {};

export const Alert = (state = INITIAL_STATE, action) => {
    switch (action.type) {
        case "":

            break;

        default:
            break;
    }

    return state;
}